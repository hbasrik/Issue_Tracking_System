# API base URL (no trailing slash).
# iOS Simulator / Android emulator (host loopback): http://localhost:8080/api/v1
# Physical device on the same LAN: http://<your-machine-lan-ip>:8080/api/v1
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
