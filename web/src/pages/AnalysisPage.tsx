// Placeholder for the Analysis tab (charts + PDF export via jspdf/html2canvas).
export default function AnalysisPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold">Analysis</h1>
      <p className="mt-2 text-slate-400">
        Charts (recharts) and PDF export (jspdf + html2canvas) will be wired here.
      </p>
    </section>
  );
}
