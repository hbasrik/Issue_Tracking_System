# Web dashboard — Vite env.
# The running app loads VITE_API_BASE_URL from the repository-root .env
# (web/vite.config.ts envDir). Copy this file to web/.env only if you need
# a web-specific override that should win over the root file.
VITE_API_BASE_URL=http://localhost:8080/api/v1
